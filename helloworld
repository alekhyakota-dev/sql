class Convert:
    def __init__(self, text):
        result = ""
        for ch in text:
            if ch >= 'a' and ch <= 'z':
                result += chr(ord(ch) - 32)
            else:
                result += ch
        print(result)
obj = Convert("hello world")
